# department module

Reserved for a future phase. Implement `department.model.ts`, `department.repository.ts`, `department.service.ts`, `department.controller.ts`, `department.routes.ts`, `department.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
