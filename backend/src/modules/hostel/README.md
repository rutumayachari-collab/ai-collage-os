# hostel module

Reserved for a future phase. Implement `hostel.model.ts`, `hostel.repository.ts`, `hostel.service.ts`, `hostel.controller.ts`, `hostel.routes.ts`, `hostel.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
