# exam module

Reserved for a future phase. Implement `exam.model.ts`, `exam.repository.ts`, `exam.service.ts`, `exam.controller.ts`, `exam.routes.ts`, `exam.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
