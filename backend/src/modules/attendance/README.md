# attendance module

Reserved for a future phase. Implement `attendance.model.ts`, `attendance.repository.ts`, `attendance.service.ts`, `attendance.controller.ts`, `attendance.routes.ts`, `attendance.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
