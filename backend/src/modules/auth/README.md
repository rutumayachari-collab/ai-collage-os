# auth module

Reserved for a future phase. Implement `auth.model.ts`, `auth.repository.ts`, `auth.service.ts`, `auth.controller.ts`, `auth.routes.ts`, `auth.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
