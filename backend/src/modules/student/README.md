# student module

Reserved for a future phase. Implement `student.model.ts`, `student.repository.ts`, `student.service.ts`, `student.controller.ts`, `student.routes.ts`, `student.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
