# parent module

Reserved for a future phase. Implement `parent.model.ts`, `parent.repository.ts`, `parent.service.ts`, `parent.controller.ts`, `parent.routes.ts`, `parent.validator.ts`, then export a `FeatureModule` from `index.ts` and register it in `src/routes/module.registry.ts`.
