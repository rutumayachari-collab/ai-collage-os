# Frontend Environment Variables
# Vite requires the VITE_ prefix for client-exposed variables.

# Production API base URL (must point to the deployed backend)
VITE_API_BASE_URL=https://your-backend-url.com/api/v1
